package lumps;

import lime.utils.Int8Array;

/**
 * ...
 * @author kevansevans
 * 
 * Scans the assets in the project folder and creates the pallete lump that GZDoom needs.
 * 
 * 3D vector of 64 unit8
 * Image needs to be converted to 6 bits per color
 * then write bytes in RGB RGB RGB
 * 
 */
class Pallet 
{
	public function new() 
	{

	}
}